class AboutController < ApplicationController
  #before_filter :authorize
  #if you want to authorize user before accessing a page, use above code but we do not want to do that for any pages

  def show
    #in views, finds directory about, then renders show
  end

end